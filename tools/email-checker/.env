# Email Manager — IMAP & SMTP Credentials
# Copy this to .env and fill in values.
#
# For Gmail:
#   1. Enable IMAP in Gmail Settings → See all settings → Forwarding and POP/IMAP
#   2. Generate an App Password at https://myaccount.google.com/apppasswords
#      (requires 2FA enabled)
#   IMAP: imap.gmail.com:993 | SMTP: smtp.gmail.com:587
#
# For Hostinger:
#   IMAP: imap.hostinger.com:993 | SMTP: smtp.hostinger.com:587
#   Use your full email address and email password

# ── Account 1: Gmail Personal (dalwaut@gmail.com) ──
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=dalwaut@gmail.com
IMAP_PASS=YOUR_GMAIL_APP_PASSWORD

# ── Account 2: Paradise Web (dallas@paradisewebfl.com) ──
IMAP_HOST_PW=imap.gmail.com
IMAP_PORT_PW=993
IMAP_USER_PW=dallas@paradisewebfl.com
IMAP_PASS_PW=YOUR_PARADISE_WEB_APP_PASSWORD

# ── Account 3: BoutaByte (dallas@boutabyte.com) ──
IMAP_HOST_BB=imap.hostinger.com
IMAP_PORT_BB=993
IMAP_USER_BB=dallas@boutabyte.com
IMAP_PASS_BB=YOUR_BOUTABYTE_PASSWORD

# ── Account 4: BoutaCare (dallas@boutacare.com) ──
IMAP_HOST_BC=imap.hostinger.com
IMAP_PORT_BC=993
IMAP_USER_BC=dallas@boutacare.com
IMAP_PASS_BC=YOUR_BOUTACARE_PASSWORD

# ── Supabase (optional — for persistent storage) ──
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_KEY=your-service-role-key

# ── OPAI workspace root (auto-detected if not set) ──
# OPAI_ROOT=/home/user/opai
