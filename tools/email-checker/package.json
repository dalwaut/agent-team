{
  "name": "opai-email-checker",
  "version": "1.0.0",
  "description": "IMAP email checker that extracts actionable tasks using Claude CLI",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node index.js --once"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "express": "^5.2.1",
    "imapflow": "^1.0.0",
    "nodemailer": "^8.0.1"
  }
}
