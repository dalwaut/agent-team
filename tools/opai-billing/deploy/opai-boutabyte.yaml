# Auto-generated by OPAI Pages Manager
# DO NOT EDIT - regenerated on Deploy Routes
http:
  routers:
    http-catchall:
      rule: "HostRegexp(`opai.boutabyte.com`)"
      entryPoints:
        - http
      middlewares:
        - redirect-to-https
      service: noop@internal

    landing-router:
      rule: "Host(`opai.boutabyte.com`) && PathPrefix(`/about`)"
      entryPoints:
        - https
      priority: 100
      service: static-pages
      middlewares:
        - landing-strip
      tls:
        certResolver: letsencrypt

    opai-catchall:
      rule: "Host(`opai.boutabyte.com`)"
      entryPoints:
        - https
      priority: 1
      service: opai-server
      tls:
        certResolver: letsencrypt

  middlewares:
    redirect-to-https:
      redirectScheme:
        scheme: https
        permanent: true

    landing-strip:
      stripPrefix:
        prefixes:
          - "/about"

  services:
    static-pages:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:8095"

    opai-server:
      loadBalancer:
        servers:
          - url: "https://100.72.206.23:443"
        serversTransport: insecure-transport

  serversTransports:
    insecure-transport:
      insecureSkipVerify: true
