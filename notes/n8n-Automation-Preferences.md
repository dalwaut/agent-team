instructions for generating automations within our envireoment & with our preferences.

AI choices:
- prefer Gemini over other AI providers

Environment:
- Cloud
- All Scheduled setups are to be run off of sub workflow call. we have a daily loop that runs and executes other automations and will just be hooking all scheduled ot repeating automations on this loop. this only applys to the start of an app.
